class AuthConfig {
  /// Google Client ID for Web targets.
  /// Generate this from GCP Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs.
  /// Set the Authorized JavaScript Origins to: http://localhost:8080 and http://127.0.0.1:8080.
  static const String googleClientIdWeb = "";

  /// Facebook App ID.
  /// Generate this from Meta for Developers -> Apps -> App Settings -> Basic.
  static const String facebookAppId = "";
}
